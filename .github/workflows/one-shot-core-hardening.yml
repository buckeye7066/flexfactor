name: one-shot-core-hardening

on:
  push:
    branches:
      - fix/predictable-runtime-failures

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: fix/predictable-runtime-failures
          fetch-depth: 0

      - name: Apply remaining reviewed hardening
        shell: bash
        run: |
          set -euo pipefail
          python scripts/finish_pr133_hardening.py
          python -m compileall -q flexfactor.py flexfactor_directed.py flexfactor_evidence.py flexfactor_runtime_hardening_tests.py
          python flexfactor_runtime_hardening_tests.py
          git diff --check

      - name: Commit verified patch and remove temporary executor
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/workflows/one-shot-core-hardening.yml scripts/finish_pr133_hardening.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add flexfactor.py flexfactor_directed.py flexfactor_evidence.py flexfactor_runtime_hardening_tests.py
          git diff --cached --check
          git commit -m 'Close remaining PR hardening review gaps'
          git push origin HEAD:fix/predictable-runtime-failures
