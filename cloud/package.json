{
  "name": "flexfactor-cloud",
  "version": "1.1.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check lib/*.js && node --check api/**/*.js"
  }
}
